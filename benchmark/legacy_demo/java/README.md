# Java benchmark — expenses

Mini Maven project mirroring the Python benchmark domain (expenses, budgets,
reports). Ships WITHOUT tests so baseline coverage starts ~30–40%.

Run: upload this directory, generate tests, run up to 3 repair iterations.
Pass when line coverage exceeds 60% (JaCoCo measurement).
